import Announcement from "@/components/theme-configs/announcement/Announcement";
import Header from "@/components/theme-configs/header/Header";
import "@/app/globals.css";
import Footer from "@/components/theme-configs/footer/Footer";
import { getThemeConfig } from "@/lib/api";
import { find } from "lodash";
import Head from "next/head";
// import * as GoogleFont from "next/font/google";

export const metadata = {
  title: "Next.js",
  description: "Generated by Next.js",
};

export default async function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  const {
    data,
    data: {
      object_config: {
        root,
        theme_settings: { children_items },
      },
    },
  } = await getThemeConfig();

  const color = find(children_items, { id: "color" });

  const typography = find(children_items, { id: "typography" });

  const styleFontBody = find(typography.children_items, { id: "style_body" });

  // const fontBase = GoogleFont["Barlow"]({
  //   subsets: ["latin"],
  //   weight: ["500"],
  //   display: "swap",
  // });

  return (
    <html lang="en" suppressHydrationWarning>
      {/* <Head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link
          rel="preconnect"
          href="https://fonts.gstatic.com"
          crossOrigin=""
        />
        <link
          href="https://fonts.googleapis.com/css?family=Barlow:500|Vast+Shadow:400|&display=swap"
          rel="stylesheet"
        />
      </Head> */}
      <body style={{ background: "var(--body_bg_color)" }}>
        <style>{`
        ${root}
        @font-face {
          font-family: Barlow;
          font-style: normal;
          font-weight: 500;
          font-display: swap;
          src: url(https://fonts.gstatic.com/s/barlow/v12/7cHqv4kjgoGqM7E3_-gs51os.woff2) format('woff2');
          unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+0304, U+0308, U+0329, U+2000-206F, U+2074, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
        }
        @font-face {
          font-family: Vast Shadow;
          font-style: normal;
          font-weight: 400;
          font-display: swap;
          src: url(https://fonts.gstatic.com/s/vastshadow/v19/pe0qMImKOZ1V62ZwbVY9dce9I9s.woff2) format('woff2');
          unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+0304, U+0308, U+0329, U+2000-206F, U+2074, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
        }
        :root {
          --primary: ${color.settings.primary_color.value};
          --font-heading: 'Vast Shadow';
          --font-body: 'Barlow'
        }
        html {
          font-size: ${styleFontBody.settings.font_base_size.value}px
        }
        body * {
          font-family: var(--font-body)
        }

        .font-heading {
          font-family: var(--font-heading)
        }
        `}</style>
        <Announcement />
        <Header />
        {children}
        {/* <Footer /> */}
      </body>
    </html>
  );
}
